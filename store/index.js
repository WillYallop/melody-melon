import playlistSlider from './modules/playlistSlider.js'

const state = () => ({
  
});

const mutations = {
  
};

const actions = {
  
};

const modules = {
  playlistSlider
};

export default {
  state,
  mutations,
  actions,
  modules,
}

